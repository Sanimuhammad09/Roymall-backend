[build]
builder = "NIXPACKS"
buildCommand = "npm run build && node_modules/.bin/prisma generate"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && npx prisma db seed && npm run start:prod"
